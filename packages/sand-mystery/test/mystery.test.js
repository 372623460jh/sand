// const { decrypt, encrypt } = require('../examples');

describe('sand-mystery.js', () => {
  test('encrypt and decrypt', () => {
    // // 加密
    // expect(encrypt()).toBeUndefined();
    // // 解密
    // expect(decrypt()).toBeUndefined();
  });
});

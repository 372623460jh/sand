import {
  getDva,
  getStore,
  setModel,
  async,
  handleRoutes,
  setRoutes,
  getRoutes,
  redirect,
  RouteLoader,
} from './src/index';

export {
  getDva, // 获取dva实例
  getStore, // 获取store
  setModel, // 设置model
  async, // 动态初始组件的方法
  handleRoutes, // 处理路由的方法
  setRoutes, // 设置路由配置
  getRoutes, // 获取路由配置
  redirect, // 重定向路由
  RouteLoader,
};
